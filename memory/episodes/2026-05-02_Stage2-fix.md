# Episode — 2026-05-02 T-38 1차 적용 (Stage2-fix)

## 개요

B-1 후속. T-38 원칙(settings.json deny 라이프사이클 설계)의 1차 적용 커밋. 브랜치: `fix/stage2-deny-lifecycle`.

**결과**: project `.claude/settings.json` deny 정합화. Global `~/.claude/settings.json` 변경은 T-39 후보로 위임.

## 해결한 문제

| # | 문제 | 해결 |
|---|------|------|
| 1 | Stage 2 deny 패턴: Write 001~007 개별 7줄 (B-1 최초 파일 생성 차단 원인) | Write globbed 1줄로 교체 (`00[1-9]*.sql`) |
| 2 | Edit 008·009 미보호 (B-1에서 새로 생성된 마이그레이션 무방비) | Edit 008·009 deny 추가 |
| 3 | decision-rules.md에 T-38 미등재 | T-32A~T-38 테이블 업데이트 |
| 4 | gotchas.md에 lifecycle 함정 미등록 | settings.json deny 라이프사이클 함정 섹션 추가 |

## 결정 (T-XX 참조)

| ID | 흡수 방식 |
|----|-----------|
| T-38 | 1차 적용: Write 7줄 → globbed 1줄, Edit 008·009 추가. project settings only (§C 제외) |

신규 결정 발급: 0건 (GR-1 정합).

## §C 제외 이유

Global `~/.claude/settings.json`에 migrations deny 미추가. Stage 2 §1.2 의도 = hooks + alwaysThinkingEnabled만. migrations 경로(`./packages/db/migrations/`)는 project-specific → global deny 추가 시 의미 과잉. T-39 후보로 위임.

## 다음 세션 인계

1. settings.json deny 라이프사이클 정합화 완료. 향후 migrations 010+ 생성 시: 파일 생성 후 `Edit(./packages/db/migrations/010*.sql)` deny 1줄 추가하면 됨.
2. Global `~/.claude/settings.json` migrations deny 추가 여부는 T-39 결정 필요.
3. 게이트 B-1 통과 확인 후 M1 진입 가능.
