# Episode — 2026-05-02 M1 Step 5 — Hooks Wrapper 검증 (Stage 2 산출물 정합)

## 개요

D+18. M1 Step 4(`5284248`) + gotcha-fix(`ef1d821`) 직후. Stage 2에서 이미 작성된 Hooks wrapper(`~/.claude/team-hooks/send-event.sh`) 4종 검증 위주 단계. 변경 0건 — 검증만.

**결과**: Step 5 Done-when 4종 모두 PASS. team-claude-assets 별도 repo 미존재 → team_claude_system/hooks/ + ~/.claude/team-hooks/ 기존 배포 구조로 충분. install.sh 멱등성 확인. Step 6 자동 진입.

---

## 검증 매트릭스 (4/4 PASS)

| # | 검증 | 명령 | 결과 |
|---|---|---|---|
| V1 | jq 있는 환경 exit 0 (서버 없어도) | Skill 페이로드 stdin → exit code | ✅ exit: 0 |
| V2 | DRY_RUN skill 이름 추출 (V2 경로 `.tool_input.skill`) | `TEAM_HOOKS_DRY_RUN=1` → stderr | ✅ `skill=my-skill` 정확 추출 |
| V3 | T-29 보너스 필드 + 일반 도구(Read) | Read 페이로드 DRY_RUN | ✅ exit: 0, tool=Read |
| V4 | install.sh 멱등성 재실행 | `bash install.sh` | ✅ `✅ Installed to ~/.claude/team-hooks` 반복 성공 |

---

## 결정 (T-XX 참조)

| ID | 흡수 |
|---|---|
| T-13 | send-event.sh wrapper 검증 완료 (stdin JSON → jq → fire-and-forget curl &) |
| T-29 | duration_ms·tool_use_id·source·success 보너스 필드 추출 정상 |
| T-14 | Skill 도구 시 `.tool_input.metadata.skill_name` → fallback `.tool_input.skill` 경로 정합 |

신규 결정 발급: 0건.

---

## 발견

### team-claude-assets 별도 repo 미존재

B_M1_구현_Plan §Step 5는 `/Users/jjaegoon/team-claude-assets/hooks/`를 참조하지만 해당 repo가 로컬에 없음. Stage 2는 hooks를 `team_claude_system/hooks/`(staging)에 작성 → `install.sh`로 `~/.claude/team-hooks/`(deployment)에 배포하는 패턴 채택. B_M1_구현_Plan의 `team-claude-assets` 경로는 plan 작성 시점의 미래 참조로 판단 — 현 단계에서는 `team_claude_system/hooks/` 구조로 충분.

→ T-39 후보 메모: team-claude-assets repo 분리 시점(M2 진입 직전?) 결정 의무.

### Self-improve 검토 (R11 메타 패턴 검토)

- Step 5 자율 발견 0건 (검증 위주 step이므로 정상 범위)
- gotcha 누적 #16 → 이번 step은 보강 step이므로 신규 발견 기대 수준 낮음
- 메타 hook 함정(#16) 발견 후 첫 검증 step → #16 자가 적용 없음 (memory 파일만 수정)

---

## 다음 세션 인계

1. **Step 6 진입 가능** (R9 자동 chain) — apps/web Vite + React + AuthProvider + ApiClient. Sonnet 모델.
2. **team-claude-assets repo**: Step 5에서 미존재 확인. M2 진입 시점 생성 검토 의무.
3. **MEMORY_INDEX 5/5 한도**: Step 5 추가 시 Step1.md → archive/ 이동.

---

## 측정값

- 신규 파일: 1 (본 episode)
- 변경 파일: 0 (검증만)
- Done-when 검증: 4/4 PASS
- 소요 시간: ~5분
