# Episode — 2026-05-02 M3 — Hooks + skill_trigger + daily_stats + asset_review_log D-B

## 개요

D+21. M2 Cowork ★★★★★ 통과 직후. M3 4 영역 자율 완료.
**147 tests PASS / 0 FAIL** (14 test files). `gate_check.sh M3` → 🚧 BLOCKED exit 2 (T-37, 사용자 어드민 검토 필수).

---

## 핵심 결정

| ID | 결정 |
|----|------|
| T-34 보류 옵션 B | claude-review-bot 비활성 유지. Phase O 재평가 입력 |
| T-31D D-B | usage_events event_type에 'review_action' 추가 + review_metadata 컬럼 (별도 테이블 X) |
| 보강 #1 | DB-level CHECK: `(event_type='review_action' AND review_metadata IS NOT NULL) OR (...)` |
| b2 | D+29 스케줄 취소 — Anthropic API key 불필요 (T-34 보류 정합) |

---

## 1 Commit

| Commit | 내용 |
|--------|------|
| `d6d9458` | [M3][T-13][T-14][T-23][T-26][T-29][T-31D] 4 영역 전체 |

---

## 변경 매트릭스 (13 파일)

| Path | Action |
|------|--------|
| `packages/db/migrations/011_usage_events_review_action.sql` | CREATE — table recreation + review_metadata + CHECK (보강 #1) |
| `packages/db/src/schema.ts` | EDIT — ReviewEventMetadata type + review_action enum + reviewMetadata column |
| `apps/api/src/schemas/hooks.ts` | CREATE — hookEventSchema Zod (T-29 보너스 필드 포함) |
| `apps/api/src/services/hooksService.ts` | CREATE — queue + 100ms/50-event flush (T-23) + skill_name→asset_id (T-14) |
| `apps/api/src/services/hooksService.test.ts` | CREATE — 11 tests |
| `apps/api/src/routes/hooksRoute.ts` | CREATE — Bearer auth + 202 |
| `apps/api/src/app.ts` | EDIT — hooksRoute mount |
| `apps/api/src/workers/statsWorker.ts` | CREATE — aggregateDaily + runDailyStatsIfDue (now param) + startStatsCron |
| `apps/api/src/workers/statsWorker.test.ts` | CREATE — 7 tests |
| `apps/api/src/services/assetStatusService.ts` | EDIT — review_action INSERT (D-B) |
| `apps/api/src/services/assetStatusService.test.ts` | EDIT — +4 D-B review_action tests (총 24) |
| `apps/api/src/index.ts` | EDIT — startStatsCron + SIGTERM clearInterval |
| `apps/api/test/e2e/hooks.e2e.test.ts` | CREATE — 7 e2e tests |

---

## 해결한 문제들

### statsWorker Date mock → Maximum call stack

vi.spyOn(global, 'Date').mockImplementation에서 `new Date(...)` 내부가 재귀 호출하여 stack overflow.
**처방**: `runDailyStatsIfDue(db, now = new Date())` — now 파라미터 직접 주입으로 mock 불필요.

### assetStatusService.test.ts usage_events 테이블 미존재

test의 in-memory DB에 usage_events 테이블이 없어 review_action INSERT 실패.
**처방**: beforeAll에 `CREATE TABLE usage_events` (review_metadata + CHECK) 추가.

### hooks.e2e.test.ts FOREIGN KEY constraint

assets INSERT 시 `author_id: 'system'` → users 테이블에 없어 FK 실패.
**처방**: `author_id: NULL` (컬럼 nullable).

---

## 테스트 커버리지 (M3 완료 시점)

| 파일 | 테스트 수 |
|------|-----------|
| searchService.test.ts | 15 |
| assetQueryService.test.ts | 21 |
| csrfGuard.test.ts | 8 |
| assetStatusService.test.ts | 24 |
| tokenService.test.ts | 8 |
| assetWriteService.test.ts | 13 |
| logger.test.ts | 3 |
| webhookWorker.test.ts | 5 |
| hooksService.test.ts | 11 |
| statsWorker.test.ts | 7 |
| health.test.ts | 1 |
| webhook-jobs.e2e.test.ts | 4 |
| hooks.e2e.test.ts | 7 |
| catalog.e2e.test.ts | 20 |
| **합계** | **147 PASS** |

---

## M3 게이트 보고서

- **결과**: BLOCKED exit 2 (T-37, 사용자 어드민 검토 필수)
- **T-34 보류**: 옵션 B 채택 — claude-review-bot 비활성. Phase O(D+45~50) 재평가 입력
- **영역**: 4/6 (T-34·T-36 보류)
- **보강 #1**: DB-level CHECK 무결성 강제 (Cowork 권고 반영)

---

## 다음 단계

M4 (대시보드) — T-37 정합으로 M3 게이트는 사용자 어드민 검토 후 진입.
R10 정합 — git push는 사용자 직접 실행.
