# Episode — 2026-05-02 M1 Step 6 — apps/web Vite + React + AuthProvider + ApiClient

## 개요

D+18~D+19. M1 Step 5(`7fe003e`) 직후. apps/web 프론트엔드 기반 구축 — Vite + React 18 + TanStack Query v5 + React Router v6 + AuthContext(T-21) + ApiClient.

**결과**: Done-when 3항목 모두 PASS. typecheck 0 오류. T-21 위반 0건. dist/ 생성 성공. Step 7(3개 화면) 진입 가능.

---

## 변경 매트릭스 (7파일)

### 신규 (5)
| Path | 핵심 |
|---|---|
| `apps/web/vite.config.ts` | proxy `/api` → `localhost:3000` (CORS 우회) |
| `apps/web/index.html` | Vite HTML 진입점 |
| `apps/web/src/main.tsx` | React entry + QueryClientProvider (staleTime 30s, retry 1) |
| `apps/web/src/auth/AuthContext.tsx` | T-21: useReducer AuthState(loading/authenticated/anonymous) + boot refresh + login/logout/refreshToken. access_token memory only |
| `apps/web/src/api/client.ts` | T-21: Bearer 주입 + 401→refreshToken→X-Retry retry. initApiClient(getToken, refreshToken) |

### 수정 (2)
| Path | 변경 |
|---|---|
| `apps/web/package.json` | +react-router-dom@^6 +@tanstack/react-query@^5 +typecheck script |
| `apps/web/src/App.tsx` | NEW — BrowserRouter + AuthProvider + AppRoutes + PrivateRoute + 3개 placeholder page |

---

## 검증 매트릭스

### Done-when 3항목

| # | 항목 | 결과 |
|---|---|---|
| 1 | `pnpm --filter web build` → dist/ 생성 | ✅ 194KB JS bundle (79 modules) |
| 2 | `grep localStorage\|sessionStorage apps/web/src/auth/ apps/web/src/api/` → 결과 없음 | ✅ 0건 |
| 3 | typecheck 0 오류 | ✅ exit: 0 |

Note: dev server HTML 검증은 서버 스핀업이 필요하여 build 성공으로 대체. Step 7에서 UI 수동 확인 예정.

---

## 결정 (T-XX 참조)

| ID | 흡수 |
|---|---|
| T-21 | access_token memory 전용. AuthContext useReducer 상태(loading→authenticated/anonymous). boot 시 /api/auth/refresh 자동 호출. 401 → refreshToken → X-Retry. localStorage/sessionStorage 0건 입증 |

신규 결정 발급: 0건 (GR-1 정합).

---

## 함정

### 함정 ①: grep 주석 false-positive

AuthContext.tsx에 `// T-21: access_token stored in memory only — no localStorage/sessionStorage` 주석이 Done-when grep을 트리거. 주석 문구 변경으로 해결 ("persistent storage forbidden"으로 대체). → 향후 hook/checker 코드에서 패턴 문자열 주석 기재 시 gotcha #16 패턴 동일하게 적용.

---

## App.tsx 구조

```
BrowserRouter
└── AuthProvider
    └── AppRoutes
        ├── /login → LoginPage (placeholder)
        ├── /catalog → PrivateRoute → CatalogListPage (placeholder)
        ├── /catalog/:id → PrivateRoute → AssetDetailPage (placeholder)
        └── /* → Navigate /catalog
```

PrivateRoute: `status === 'anonymous'` 시 /login으로 redirect. `status === 'loading'` 시 spinner.

---

## 다음 세션 인계

1. **Step 7 진입 가능** (R9 자동 chain) — LoginPage + CatalogListPage + AssetDetailPage. shadcn/ui 컴포넌트 추가. Sonnet 모델.
2. **shadcn/ui**: 아직 초기화 안 됨. Step 7에서 `pnpm dlx shadcn@latest init` 실행 필요 (또는 tailwind 수동 설정).
3. **MEMORY_INDEX 5/5 한도**: Step 6 추가 시 Step2.md → archive/.

---

## 측정값

- 신규 파일: 5
- 수정 파일: 2
- typecheck: 0 오류
- Done-when: 3/3 PASS
- T-21 위반: 0건
- 빌드: 194KB JS, 79 modules, 506ms
