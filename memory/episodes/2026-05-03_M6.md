# Episode — 2026-05-03 M6 — 카탈로그 사용자 경험 정합 회복 (U-Mj-1·2·3·5 + S10)

## 개요

D+23 (Phase O). R-G9 페르소나 영역 라운드 결과 5건 결손 정합 회복.
**211 tests PASS / 0 FAIL** (24 test files). `precommit-check.sh` 7/7 PASS, `overall_ok: true`.

---

## 결손 → 정합 매트릭스

| ID | 결손 | 구현 | 상태 |
|----|------|------|------|
| U-Mj-1 | 카탈로그 정렬 파라미터 미동작 | sort enum 확장 (last_updated·view_count·download_count) + sort 드롭다운 UI | ✅ |
| U-Mj-2 | 리뷰 완료 인앱 알림 없음 | notificationService + notifications route + assetStatusService hook | ✅ |
| U-Mj-3 | 카테고리/태그 탐색 없음 | getTags() + GET /assets/tags + CategoryBrowser UI + tag 필터 | ✅ |
| U-Mj-5 | 인앱 피드백 채널 없음 | feedbackService + feedback route + FeedbackModal + AssetDetailPage 버튼 | ✅ |
| S10 | 시스템 수준 피드백 없음 | feedback.asset_id nullable + CatalogListPage footer | ✅ |

---

## 사전 결정 사항

| # | 결정 |
|---|------|
| D-7 | 알림 = 인앱 (이메일 Phase 2) |
| D-8 | 피드백 = feedback 테이블 재구성 (GitHub Issue Phase 2) |
| D-9 | Migration 013 notifications + 014 feedback_v2 분리 |
| D-10 | sort=view_count/download_count cursor = id 기반 (MVP) |
| D-11 | GET /assets/tags literal route before /:id (D-4 정합) |
| D-12 | 알림 배지 30s 폴링 |

---

## Cowork 자율 발견 처리

| ID | 내용 | 처리 |
|----|------|------|
| C-1 ★★★★ | Notification RBAC — 타인 알림 차단 | `WHERE id=? AND user_id=?` 적용 ✅ |
| C-2 | Feedback 스팸 방지 | `content min 10자` Zod 검증 ✅ |
| C-3 | 빈 태그 필터 | `AND t.value != ''` ✅ |
| C-4 | sort localStorage 저장 | Phase O 백로그 (미구현) |

---

## 1 Commit (예정)

| Commit | 내용 |
|--------|------|
| M6 | [M6][U-Mj-1·2·3·5+S10] 카탈로그 사용자 경험 정합 회복 |

---

## 변경 매트릭스 (22 파일)

### 신규 (13)
| Path | 내용 |
|------|------|
| `packages/db/migrations/013_notifications.sql` | notifications 테이블 + 인덱스 2종 |
| `packages/db/migrations/014_feedback_v2.sql` | feedback 재구성 (table recreation 패턴) |
| `apps/api/src/schemas/notification.ts` | Zod 스키마 |
| `apps/api/src/schemas/feedback.ts` | Zod 스키마 (C-2: min 10자) |
| `apps/api/src/services/notificationService.ts` | listNotifications (C-1 RBAC) + markRead (C-1 WHERE AND) |
| `apps/api/src/services/feedbackService.ts` | createFeedback + listFeedback |
| `apps/api/src/routes/notifications.ts` | GET /notifications + PATCH /:id/read |
| `apps/api/src/routes/feedback.ts` | POST /feedback + GET /feedback (reviewer+) |
| `apps/api/src/tests/sortTags.test.ts` | 6케이스 (sort 3종 + tag 2종 + getTags) |
| `apps/api/src/tests/notifications.test.ts` | 5케이스 (C-1 RBAC 포함) |
| `apps/api/src/tests/feedback.test.ts` | 4케이스 (S10 포함) |
| `apps/web/src/components/CategoryBrowser.tsx` | 태그 그리드 컴포넌트 |
| `apps/web/src/components/FeedbackModal.tsx` | 피드백 모달 (asset + system) |
| `apps/web/src/pages/NotificationsPage.tsx` | 알림 목록 페이지 + mark-read |

### 수정 (8)
| Path | 변경 |
|------|------|
| `packages/db/src/schema.ts` | notifications + feedback v2 재정의 |
| `apps/api/src/schemas/asset.ts` | sort enum 확장 |
| `apps/api/src/services/assetQueryService.ts` | getTags() + tag 필터 + count 정렬 |
| `apps/api/src/routes/assets.ts` | GET /assets/tags 등록 (/:id 앞) |
| `apps/api/src/services/assetStatusService.ts` | notification hook (fire-and-forget) |
| `apps/api/src/app.ts` | notificationsRoute + feedbackRoute mount |
| `apps/web/src/pages/CatalogListPage.tsx` | sort 드롭다운 + CategoryBrowser + 알림 배지 + footer |
| `apps/web/src/pages/AssetDetailPage.tsx` | 피드백 버튼 + FeedbackModal |
| `apps/web/src/App.tsx` | /notifications 라우트 추가 |

---

## 테스트 커버리지

| 파일 | 테스트 수 |
|------|-----------|
| sortTags.test.ts (신규) | 6 |
| notifications.test.ts (신규) | 5 |
| feedback.test.ts (신규) | 4 |
| (M5+Hotfix 이전 합계) | 196 |
| **합계** | **211 PASS** |

---

## 핵심 설계 주의사항

### gotcha #18 준수
- `getTags()`: `ORDER BY count DESC, t.value ASC`
- `listAssets()` count sort: `ORDER BY sort_count DESC, a.id DESC`
- `listNotifications()`: `ORDER BY created_at DESC, id DESC`
- `listFeedback()`: `ORDER BY created_at DESC, id DESC`

### C-1 RBAC (★★★★)
- `listNotifications()`: `WHERE user_id = ?` — 본인 알림만
- `markRead()`: `WHERE id = ? AND user_id = ?` — 타인 알림 갱신 차단
- 403 대신 404 반환 (사용자에게 타인 알림 존재 여부 노출 방지)

### notification hook (assetStatusService.ts)
- transaction 외부 fire-and-forget (notifications 테이블 없어도 무음 실패)
- 발화 조건: `newStatus === 'approved'` 또는 `existing.status === 'in_review' && newStatus === 'draft'`
- assetStatusService.test.ts의 기존 테스트: notifications 테이블 없어도 catch로 처리 → 회귀 없음

---

## 다음 단계

- vault §2.27 갱신 의무 (R7)
- 00_INDEX.md 갱신 (GR-7: 신규 파일 14개)
- R10 정합 — git push는 사용자 직접 실행
- Phase O 백로그 추가: C-4 sort localStorage + /notifications 알림 배지 별도 query key

