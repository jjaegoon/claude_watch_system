# Episode — 2026-05-02 M1 Step 1 — packages/db Schema·Migrations 정합 검증

## 개요

D+14. M1 Step 0(`1bb9ca3`) + gotcha-fix(`34eeb48`, amend됨) 직후 Step 1 검증 위주 단계 진입. 마스터 §Step 1 (L59~100) Done-when 5명령 verbatim 통과 + PRAGMA 4종 정량 활성 + FTS5 트리거 3종 INSERT/UPDATE/DELETE 정량 동기화 + schema·migrations vs 마스터 spec 1:1 정합 입증. **schema/migrations 변경 0건** — 분기 H.1(episode + INDEX commit) 채택.

**결과**: 모든 검증 PASS. B-1 산출물 결함 부재 확인. Step 2(apps/api Hono) 진입 가능.

---

## 검증 매트릭스 (전체 PASS)

### §B 마스터 Done-when (verbatim 5명령)

| # | 명령 | 기대 | 실제 | 결과 |
|---|---|---|---|---|
| B.1 | `pnpm --filter @team-claude/db migrate` | 9 idempotent skip | 9/9 skipped (already applied) | ✅ |
| B.2 | `sqlite3 data/dev.db ".tables"` | 9 user + __migrations + assets_fts*5 | 정합 (assets/asset_versions/assets_fts/assets_fts_*5/daily_asset_stats/daily_user_stats/feedback/users/usage_events/webhook_jobs/__migrations) | ✅ |
| B.3 | `sqlite3 data/dev.db ".schema assets_fts"` | virtual table fts5 + unicode61 remove_diacritics 1 | `CREATE VIRTUAL TABLE assets_fts USING fts5(asset_id UNINDEXED, name, description, tags, tokenize = 'unicode61 remove_diacritics 1')` | ✅ |
| B.4 | `sqlite3 ... type='trigger'` | assets_ai/au/ad | 3 정상 | ✅ |
| B.5 | `PRAGMA journal_mode` | wal | wal | ✅ |

### §C PRAGMA 4종 (단일 connection 시뮬레이션)

| # | PRAGMA | 분류 | 기대 | 실제 | 결과 |
|---|---|---|---|---|---|
| C.1 | journal_mode | persistent (DB 파일 영속) | wal | wal | ✅ |
| C.2 | busy_timeout | connection-scoped (set 후 query) | 5000 | 5000 | ✅ |
| C.3 | synchronous | persistent | 1 (NORMAL) | 1 | ✅ |
| C.4 | foreign_keys | connection-scoped (set 후 query) | 1 (ON) | 1 | ✅ |

**중요**: SQLite의 `busy_timeout`/`foreign_keys`는 connection-scoped — 새 연결마다 default reset. dev.db 파일 자체에 영속되는 PRAGMA는 `journal_mode`/`synchronous` 2종만. application connection(client.ts)에선 4 PRAGMA 모두 set:

```ts
// packages/db/src/client.ts
sqlite.pragma('journal_mode = WAL')
sqlite.pragma('busy_timeout = 5000')
sqlite.pragma('synchronous = NORMAL')
sqlite.pragma('foreign_keys = ON')
```

검증은 sqlite3 CLI 단일 connection에서 set + query로 동작 입증.

### §D FTS5 트리거 정량 동기화 (한국어 토크나이저 + json_each tags)

| # | 트리거 | 시나리오 | 기대 assets_fts | 실제 | 결과 |
|---|---|---|---|---|---|
| D.0 | (cleanup) | DELETE WHERE id LIKE 'verify-%' | 0 rows | 0 | ✅ |
| D.1 | assets_ai (INSERT) | INSERT '코드 리뷰' + tags `["review","ai"]` | name='코드 리뷰' / description='한국어 설명 테스트' / tags='review ai' | 정확 일치 | ✅ |
| D.2 | assets_au (UPDATE) | UPDATE name='코드 리뷰 v2' + tags `["review","updated"]` | name='코드 리뷰 v2' / description='갱신된 설명' / tags='review updated' | 정확 일치 | ✅ |
| D.3 | assets_ad (DELETE) | DELETE FROM assets WHERE id='verify-fts-1' | COUNT=0 | 0 | ✅ |
| D.4 | (cleanup) | 사후 DELETE | 0 잔여 | 0 | ✅ |

**검증된 동작**:
- json_each + GROUP_CONCAT tags 분해: `["review","ai"]` → `'review ai'` (공백 구분 토큰)
- unicode61 remove_diacritics 1 한국어 토큰화: '코드 리뷰' 정상 저장 (NFC 정규화)
- 3 트리거 모두 commit 후 즉시 동기화 (tx-coupled)

### §E·F·G

| # | 항목 | 결과 |
|---|---|---|
| E | `git diff --stat HEAD packages/db/` | empty (변경 0) ✅ |
| F | `pnpm --filter @team-claude/db test` | schema.test.ts: all assertions passed ✅ |
| G | `bash evals/gate_check.sh B-1` | ✅ Gate B-1 자동 통과 (cherry-pick 무결성 재입증) |

---

## schema·migrations vs 마스터 spec 1:1 정합 (사전 read-only 검증 표)

| 마스터 §Step 1 명시 | 실제 | 정합 |
|---|---|---|
| schema.ts 8 테이블 (users·assets·assetVersions·usageEvents·feedback·dailyAssetStats·dailyUserStats·webhookJobs) | 8 모두 정의 | ✅ |
| webhookJobs (T-15) | source/payload/status enum 5종/retryCount/lastError/createdAt/startedAt/completedAt + statusIdx | ✅ |
| asset_versions (T-20) | snapshot JSON + onDelete cascade + assetCreatedIdx | ✅ |
| UsageEventMetadata (T-29) | skill_name/tool_use_id/duration_ms/success/source + [key:string] catchall | ✅ |
| WAL client.ts | 4 PRAGMA 모두 명시 (journal_mode·busy_timeout·synchronous·foreign_keys) | ✅ |
| utils.ts | toRating/fromRating ×100 + buildFts5Query는 Step 4 위임 주석 | ✅ |
| 마이그레이션 001~009 | 9개 모두 존재 (settings.json deny 보호 활성) | ✅ |
| 008 V3 json_each 트리거 | json_each + GROUP_CONCAT 패턴 + DROP IF EXISTS 멱등 | ✅ |
| 009 schema only | webhook_jobs + statusIdx (worker M2 위임) | ✅ |
| drizzle.config.ts | dialect sqlite + url 정합 | ✅ |
| schema.test.ts | T-34 system_user/isBot + UsageEventMetadata + utils 왕복 | ✅ |
| T-34 users.role enum | member/reviewer/admin/system_user 4종 + is_bot boolean | ✅ |
| T-32A scenario_tag | UsageEventMetadata.tool_use_id 필드 (신규 컬럼 X) | ✅ |

**누락 항목**: 0건. 마스터 spec 모든 항목이 B-1 산출물에 정합.

---

## 결정 (T-XX 참조)

| ID | 검증 결과 |
|----|-----------|
| T-15 | webhook_jobs 테이블 + status enum 5종 + statusIdx 모두 정합. worker는 M2 위임 |
| T-19 | FTS5 가상 테이블 + 트리거 3종 + 한국어 토크나이저 + json_each 모두 정합 |
| T-20 | asset_versions snapshot + cascade + index 정합 |
| T-23 | usage_events 테이블 + 4 인덱스 정합. 100ms flush 로직은 M3 위임 |
| T-26 | daily_asset_stats + daily_user_stats + uq_daily_stats unique index 정합 |
| T-27 | buildFts5Query는 utils.ts 주석에 Step 4 위임 명시 — 본 Step 검증 X |
| T-29 | UsageEventMetadata 5 필드 + catchall 정합 |
| T-32A | tool_use_id metadata 활용 (신규 컬럼 추가 X) |
| T-34 | system_user role + is_bot boolean 정합 |

신규 결정 발급: 0건 (GR-1 정합).

---

## 변경 (분기 H.1 — schema 변경 0)

| Path | Action | 사유 |
|---|---|---|
| `memory/episodes/2026-05-02_M1_Step1.md` | CREATE | 본 episode |
| `memory/episodes/2026-05-02_Stage2.md` | RENAME → `archive/` | MEMORY_INDEX 5/5 한도 유지 (Stage2가 가장 오래된 episode) |
| `memory/MEMORY_INDEX.md` | EDIT | M1_Step1 추가, Stage2 라인 제거 |

**미수정** (검증만): `packages/db/**`, `apps/**`, `evals/**`, `hooks/**`, `.claude/**`, 루트 `package.json`/`pnpm-workspace.yaml`/`tsconfig.json`. GR-1 정합 (B-1 결정 immutable).

---

## 누락 보강 결정 (분기 H.2 비활성)

본 Step에서 schema·migrations 변경은 GR-1으로 금지. 다음 보강 후보 검토:

| 후보 | 결정 |
|---|---|
| FTS5 트리거 단위 테스트를 schema.test.ts에 흡수 | **불필요** — sqlite3 CLI 정량 검증으로 §D 충족. in-memory better-sqlite3 FTS5 동작 확인은 dist 빌드·tsx 인터프리터 의존 (현재 미설정) |
| schema.test.ts에 webhook_jobs status enum 5종 런타임 검증 | **불필요** — 컴파일 타입 검증(InferSelectModel)으로 enum 5종 입증. 런타임 insert는 가치 中 |
| toRating/fromRating 경계 케이스(negative/큰 정수) | **불필요** — 단순 산술 함수, 기본 케이스로 충분 |

**결론**: 사용자 종료 조건 명시 "변경 없으면 skip 또는 단위 테스트만"의 첫 번째 옵션 채택. schema.test.ts는 T-34·T-29·utils 핵심 영역 커버하며 정합 입증 충족.

---

## 새 발견 / Gotcha 후보

### SQLite PRAGMA 분류 (gotcha #8 후보)

**관찰**: `busy_timeout`/`foreign_keys`는 connection-scoped — 새 sqlite3 CLI 연결에서 default reset. 영속 검증 위해 set+query를 같은 connection에서 실행 필요.

**처방**: PRAGMA 검증 시 sqlite3 heredoc으로 단일 connection 사용:
```bash
sqlite3 data/dev.db <<'SQL'
PRAGMA busy_timeout = 5000;
PRAGMA foreign_keys = ON;
SELECT timeout FROM pragma_busy_timeout;     # 5000
SELECT foreign_keys FROM pragma_foreign_keys;# 1
SQL
```

**현재 등재 여부**: 본 episode에 명시. gotchas.md 등재는 향후 Step 시 발견 빈도 따라 결정.

---

## 다음 세션 인계

1. **Step 2 진입 가능** — apps/api Hono 기반 + 미들웨어 + /health (마스터 §Step 2 L104~160). Sonnet 라우팅.
2. **B-1 schema·migrations는 immutable** — Step 2 이후에도 변경 금지. 새 마이그레이션은 010+ (settings.json deny 정합).
3. **FTS5 트리거는 검증 완료** — Step 4 GET /assets에서 buildFts5Query만 추가하면 카탈로그 검색 동작.
4. **MEMORY_INDEX 5/5 한도 도달** — Step 2 episode 추가 시 가장 오래된 `2026-05-02_Stage2-fix.md`를 archive 이동 필요.

---

## 측정값

- 신규 파일 (Step 1): 1 (본 episode)
- RENAME 파일: 1 (Stage2 → archive/)
- 수정 파일: 1 (MEMORY_INDEX.md)
- schema·migrations 변경: 0 (분기 H.1)
- 검증 통과: 마스터 §Step 1 5명령 + PRAGMA 4종 + FTS5 3 트리거 + schema diff + schema.test + gate B-1 = 14항 PASS
- 신규 commit: 1 예정 (`[I-1][T-15][T-19][T-20] M1 Step 1`)
- 빌드 시간: ~5분 (검증 위주)
