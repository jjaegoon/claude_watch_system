# Episode — 2026-05-02 Stage 2 자율 빌드 인프라 구축

## 개요

D+11+α. Phase D 완료 + Stage 1 자율 빌드 결정 9건(T-32A~T-37) 흡수. 본 세션에서 Stage 2 인프라 7종 + 8축 종료 게이트 구축 완료.

**결과**: gate_check.sh B-2 자율 통과 (13/13 테스트). Stage 3 B-1 진입 가능.

## 해결한 문제

| # | 문제 | 해결 |
|---|------|------|
| 1 | 자율 빌드 인프라 부재 (hooks·sub-agents·memory·evals 모두 미존재) | 7섹션 ~40 파일 신규 작성 |
| 2 | git repo 미초기화 | `git init -b main` + .gitignore 작성 |
| 3 | precheck-plan.sh 셀프-브릭 가능성 | §2 hooks를 §1 settings보다 먼저 배포 (R1 순서) |
| 4 | ~/.claude/settings.json 기존 키 손실 위험 | `.bak.<ts>` 백업 + jq `+` 머지 (alwaysThinkingEnabled 보존 확인) |
| 5 | scan-secrets.js 글로브 `**/`가 zero-dirs 미매칭 (false positive) | per-character globToRegex로 재구현, `**/` → `(?:.*/)?` |
| 6 | scanner-allowlist.ts 주석에 `paths: [...]`가 regex 거짓 매칭 → allowlist 빈 채로 로드 | 주석 텍스트 변경 |
| 7 | jq `select(. != "")` 빈 스트림 → 객체 전체 사라짐 (send-event.sh 결정적 버그) | `if ... then null else ... end` 패턴 |
| 8 | precommit-check.sh stat octal-as-decimal 비교 모호 | 엄격 일치 (`!= "755"`) |
| 9 | claude-review.yml에 PR 제목·본문 expression injection 취약점 | env 블록으로 변수 격리 |
| 10 | llm_judge_monthly.py NotImplementedError unhandled | try/except + activation_pending 시그널 |
| 11 | regression_test.py subprocess 실패가 skip와 구별 불가 | RUN_INTEGRATION 명시 분기 |
| 12 | baseline_scores.json 키 충돌 (LLM judge `T-XX` vs regression `auth/...`) | `llm_judge_scores` 별도 namespace |

## 결정 (T-XX 참조)

| ID | 흡수 방식 |
|----|-----------|
| T-13 | hooks/send-event.sh — 명세 verbatim, dry-run 모드 추가 |
| T-13 staging | **R10 신규 결정 부속** — repo `hooks/`에서 staging, install.sh로 deployment. T-13 verbatim path 변경 아님 (gotchas.md 등록) |
| T-14 | hooks_receive.json + skill_trigger.json 골든셋 등록 |
| T-15·T-19·T-23·T-27·T-29 | 골든셋 + 러너 4종 + 회귀 베이스라인 |
| T-32A | skill_trigger.json scenario_tag 분기 (registered/unregistered) |
| T-32B | hooks/precheck-plan.sh 별도 스크립트 |
| T-32C | hooks/precommit-check.sh 통합 7-검사 + JSON 출력 |
| T-32D | hooks/scan-secrets.js + scanner-allowlist.ts (TS 상수, audit=git history) |
| T-33 | claude-review.yml D+29 활성화 가드 + ANTHROPIC_API_KEY 미발급 dry-run |
| T-34 | review-subagent.md 5축 JSON contract |
| T-36 | llm_judge_monthly.py 5축 판정 + activation_pending 시그널 |
| T-37 | gate_check.sh B-1·B-2·M2 자동 / M1·M3·M4 BLOCKED exit 2 |

신규 결정 발급: 0건 (GR-1 정합).

## 실패 시도 / Self-correction (review-subagent 라운드 2회)

### Round 1 — §2 hooks (4 blockers, 5 majors, 5 minors)

발견·즉시 수정:
1. send-event.sh EVENT_JSON 빈 문자열 검증 누락 → 검증 추가
2. scan-secrets.js globToRegex `**` 처리 버그 → per-character 재작성
3. precommit-check.sh stat 비교 octal-as-decimal → 엄격 일치
4. scan-secrets.js stdin 동기/비동기 혼용 → fs.readFileSync(0) 통일

후속 대응 (majors): staged blob 검사로 변경, RESULTS_JSON 방어적 빌드, 화이트리스트 silent failure 경고, 화이트리스트 ASIA/AGPA 등 추가, install.sh atomic checksums.

### Round 2 — §1 §3 §5 §6 (4 blockers, 4 majors, 4 minors)

발견·즉시 수정:
1. claude-review.yml expression injection (PR title/body) → env 블록
2. claude-review.yml ANTHROPIC_API_KEY 직접 expression interpolation → env 변수 참조
3. llm_judge_monthly.py NotImplementedError unhandled → try/except + 시그널
4. regression_test.py subprocess silent skip → RUN_INTEGRATION 명시 분기

후속 대응 (majors): baseline_scores.json key namespace 분리, hooks/precommit-check.sh 존재 가드, hooks-dry-run.sh test 12·13 추가 (.checksums.json + scanner-allowlist 검증), GNU grep 포팅 주석.

후속 대응 (minors): JSON parse 예외 처리, review-subagent.md axis 4 다언어 일반화 (TS/Python/Bash).

## 다음 세션 인계

1. **Stage 3 B-1 진입 가능**. B-1은 DB schema (assets, assets_fts, webhook_jobs, users, usage_events) + WAL 모드 + 마이그레이션 008·009 (T-19 FTS5, T-15 webhook_jobs) 작성.
2. **B-1 완료 후 gate_check.sh B-1 자동 검증** (현재 SKIP — packages/db/migrations 비어있음).
3. **claude-review.yml 활성화 트리거**: D+29 (M3 진입). ANTHROPIC_API_KEY 발급 후 `pull_request:` 트리거 주석 해제.
4. **GitHub remote 미설정**. Stage 3 또는 그 이후 사용자가 `git remote add origin ...` 설정 시 push 가능. 현재는 로컬 commit만.
5. **memory/episodes/ 다음 에피소드**: B-1 완료 후 `2026-05-XX_B-1.md` 작성. 6개 누적 시 가장 오래된 것 archive로 이동.

## 새 Gotcha (gotchas.md 등록 항목)

이미 gotchas.md에 등록 완료된 항목:
- T-13 staging deviation (repo hooks/ vs ~/.claude/team-hooks/)
- scan-secrets globToRegex `**/` zero-dirs 처리
- scanner-allowlist.ts 주석에 `paths: [...]` 텍스트 금지
- jq `select(. != "")` 빈 스트림 함정
- precommit-check.sh octal-as-decimal 비교 모호
- claude-review.yml PR title/body expression injection (env 블록 필수)

## 측정값

- 신규 파일: ~40 (정확히는 39 authored + 1 generated .checksums.json + 1 modified ~/.claude/settings.json)
- gate_check.sh B-2: 13/13 테스트 통과
- precommit-check.sh: 7/7 검사 PASS (overall_ok=true)
- review-subagent 라운드: 2회 (총 8 blockers + 9 majors + 9 minors 발견·수정)
- 빌드 시간: ~3시간 (탐색 + 플랜 + 빌드 + 2 라운드 self-improve)
