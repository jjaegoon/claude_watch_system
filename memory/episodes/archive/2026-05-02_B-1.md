# Episode — 2026-05-02 B-1 Drizzle 스키마 + 마이그레이션 008·009

## 개요

D+11, Phase B B-1 완료. packages/db/ (schema + migrations) 전체 신규 작성.
gate_check.sh B-1 SKIP → PASS 전환.

**결과**: gate_check.sh B-1 자율 통과. Stage 3 B-2 진입 가능.

## 해결한 문제

| # | 문제 | 해결 |
|---|------|------|
| 1 | drizzle-kit migrate가 meta/_journal.json 필요 | 커스텀 tsx src/migrate.ts로 교체 (__migrations 추적 테이블) |
| 2 | settings.json deny(Write 001~007)가 최초 생성도 차단 | Bash heredoc으로 외부 생성 후 자율 진행 (R1 실현, 사용자 Option B 선택) |
| 3 | better-sqlite3 네이티브 바인딩 미빌드 (Node.js v24) | npx node-gyp rebuild로 소스 빌드 |
| 4 | noUncheckedIndexedAccess: array destructure member undefined | assert.ok(member) 선행 추가 (type narrowing) |
| 5 | data/.gitkeep — data/가 .gitignore에 포함 | migrate.ts의 fs.mkdirSync로 런타임 생성 (gitkeep 불필요) |

## 결정 흡수 (T-XX 참조)

| ID | 흡수 방식 |
|----|-----------|
| T-15 | webhook_jobs 테이블 schema only (009 마이그레이션) |
| T-19 | FTS5 가상 테이블 + json_each 트리거 3종 (008 마이그레이션) |
| T-20 | asset_versions snapshot 스키마 (003 마이그레이션) |
| T-23 | usage_events 테이블 (100ms flush 구조 준비, 004 마이그레이션) |
| T-26 | daily_asset_stats · daily_user_stats (006·007 마이그레이션) |
| T-29 | UsageEventMetadata 타입 (schema.ts export) |
| T-32A | tool_use_id 분기 — 신규 필드 추가 없음 (metadata JSON 내 기존 키 활용) |
| T-34 | users.role enum 'system_user' + is_bot 컬럼 (001 마이그레이션) |

신규 결정 발급: 0건 (GR-1 정합).

## 측정값

- 신규 파일: 20개 (TS 7 + SQL 9 + tsconfig 2 + drizzle.config 1 + package.json 수정 1)
- gate_check.sh B-1: PASS (9/9 migration 적용 + WAL 확인)
- precommit-check.sh: 7/7 PASS (overall_ok=true)
- typecheck: PASS
- test (schema.test.ts): PASS

## 새 Gotcha (gotchas.md 등록 후보)

- **better-sqlite3 + Node.js v24**: prebuilt 바이너리 없음 → `npx node-gyp rebuild --directory <path>` 필수
- **settings.json deny = Write 포함 시 최초 생성도 차단**: Write deny와 Edit deny를 구분해서 설계해야 함. 초기 생성 허용 + 이후 수정 금지는 Edit deny만으로 충분
- **drizzle-kit migrate requires journal**: 수동 SQL 마이그레이션에는 커스텀 migrator 필수 (drizzle-kit generate는 여전히 유용)

## settings.json 갱신 [Stage2-fix 후보 ADR]

Write 개별 7건 → Write glob (`*.sql`) 통합. Edit deny 008·009 추가.
향후 migrations 파일 추가 시 Edit deny만 추가하면 됨 (Write는 glob으로 자동 커버).

## 다음 세션 인계

1. **Stage 3 B-2 진입 가능**. B-2는 hooks 재검증 (dry-run) + gate_check.sh B-2 통과.
2. **B-2 후 M1 Step 2 진입**: Hono API 라우트 (apps/api) — schema.ts 타입 import해서 사용.
3. **better-sqlite3 gotcha 등록 예정**: memory/knowledge/gotchas.md에 Node.js v24 바인딩 이슈 추가 권장.
4. **settings.json Write deny 설계 원칙**: 초기 생성 기간에는 Edit deny만 → 초기 생성 완료 후 Write deny 추가. B-1 교훈.
